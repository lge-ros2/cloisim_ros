syntax = "proto2";
package cloisim.msgs;

/// \ingroup cloisim_msgs
/// \interface Plugin
/// \brief A message containing visual information for cloisim::Plugin

message Plugin
{
  required string name         = 1;
  required string filename     = 2;
  optional string innerxml     = 3 [default = ""];
}
